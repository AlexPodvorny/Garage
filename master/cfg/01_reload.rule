# RULE 1 Reload (action)
MATCHED_RULE1='0'
if [[ $ACTION_RECEIVED == "Reload" ]]
then
 MATCHED_RULE1="1"
fi


# RULE 1 ACTIONS
if [[ "$MATCHED_RULE1" == "1" ]]
then

#Action 1.1 (custom) 
echo "" > $DATA_PATH/reboot
fi
