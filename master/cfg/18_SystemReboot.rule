# RULE 18 SystemReboot (action)
MATCHED_RULE18='0'
if [[ $ACTION_RECEIVED == "SystemReboot" ]]
then
 MATCHED_RULE18="1"
fi


# RULE 18 ACTIONS
if [[ "$MATCHED_RULE18" == "1" ]]
then

#Action 18.1 (custom) 
reboot
fi
